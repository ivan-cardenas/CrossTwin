* Add numbering to the steps or demo - Start here: Select topic - Then Explore current Data - Then explore Indicators - Then selection of scenarios (sliders and buttons) - Then import data  - Then run analysis - Then view results - Then export results

* Aggregation of data per H3 hexagon - Add H3 hexagon configuration for spatial aggregation (`core/utils.py`)

* Add H3 hexagon aggregation to `common/models.py`

* Have overview of active layers

* Add policy options and restriction in pop up